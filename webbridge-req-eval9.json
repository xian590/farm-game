{"action":"evaluate","args":{"code":"(() => { try { const saveGameSrc = saveGame.toString().substring(0, 500); const saveGameManualSrc = saveGameManual.toString().substring(0, 500); const keys = Object.keys(window).filter(k => k.toLowerCase().includes('state') || k.toLowerCase().includes('game') || k.toLowerCase().includes('save')); return JSON.stringify({saveGameSrc, saveGameManualSrc, potentialVars: keys.slice(0, 30)}); } catch(e) { return JSON.stringify({error: e.message}); } })()"},"session":"farm-game"}

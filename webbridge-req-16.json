{"action":"evaluate","args":{"code":"(() => { const money = document.getElementById('stat-money')?.textContent; const topText = document.querySelector('.top-bar')?.innerText || ''; const staminaMatch = topText.match(/([0-9]+)\s*\/\s*([0-9]+)/); const stamina = staminaMatch ? staminaMatch[0] : null; const healthMatch = topText.match(/([0-9]+)\s*\/\s*([0-9]+)/g); const health = healthMatch && healthMatch[1] ? healthMatch[1] : null; const dayMatch = topText.match(/第\s*([0-9]+)\s*天/); const day = dayMatch ? dayMatch[1] : null; const fields = document.querySelectorAll('.field-card').length; const speed = document.querySelector('.speed-btn-top.active')?.textContent; const modal = document.querySelector('.modal-overlay'); const modalStyle = modal ? window.getComputedStyle(modal).display : 'none'; const modalVisible = modalStyle !== 'none'; return JSON.stringify({ money: money, stamina: stamina, health: health, day: day, fields: fields, speed: speed, modalVisible: modalVisible }); })()"},"session":"farm-test"}

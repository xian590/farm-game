{
  "action": "evaluate",
  "args": {
    "code": "(() => { const tabs = document.querySelectorAll('.sidebar-tab'); for (let tab of tabs) { if (tab.textContent.includes('商店')) { tab.click(); return 'clicked shop'; } } return 'shop not found'; })()"
  },
  "session": "farm-game"
}

{"action":"evaluate","args":{"code":"(() => { const errorLog = localStorage.getItem('farm_game_error_log'); const speed = localStorage.getItem('farm_game_speed'); const allKeys = Object.keys(localStorage); const saveData = allKeys.filter(k => k.includes('save') || k.includes('farm')).map(k => ({key: k, size: localStorage.getItem(k)?.length || 0})); return JSON.stringify({ speed: speed, allKeys: allKeys, saveData: saveData, bodyText: document.body.innerText.slice(0,500), title: document.title, scripts: Array.from(document.scripts).map(s => s.src).filter(Boolean) }); })()"},"session":"farm-game"}

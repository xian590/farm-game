{
  "action": "evaluate",
  "args": {
    "code": "(() => { const tabs = document.querySelectorAll('.sidebar-tab'); for (let tab of tabs) { if (tab.textContent.includes('背包')) { tab.click(); return 'clicked backpack'; } } return 'backpack not found'; })()"
  },
  "session": "farm-game"
}
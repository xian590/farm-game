{"action":"evaluate","args":{"code":"(() => { const keys = Object.keys(window).filter(k => typeof window[k] === 'object' && window[k] !== null && (k.includes('game') || k.includes('farm') || k.includes('app'))); const localKeys = Object.keys(localStorage); const saves = localKeys.filter(k => k.includes('farm') || k.includes('save')); return JSON.stringify({ globalKeys: keys, localStorageKeys: localKeys.slice(0,20), saveKeys: saves }); })()"},"session":"farm-game"}
